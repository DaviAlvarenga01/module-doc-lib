/**
 * index.ts (controllers)
 * 
 * Índice de exportação de todas as funções de controller.
 * Controllers representam a camada de orquestração na arquitetura MVC.
 * 
 * Processos:
 * - Exporta generate como função principal de orquestração
 */

export { generate } from './ModuleGeneratorController.js';
